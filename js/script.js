/*
const canvas = document.getElementById("gameCanvas");
const ctx = canvas.getContext("2d");

// Initialize game variables
let playerX = canvas.width / 2;
let playerY = canvas.height / 2;
let playerSpeed = 5;


// Main game loop
function gameLoop() {
    clearCanvas();
    update();
    draw();
    requestAnimationFrame(gameLoop);
}

// Clear the canvas
function clearCanvas() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
}

// Update game state
function update() {
    // Update player position based on input (e.g., keyboard input)
    // Update other game elements (enemies, asteroids, etc.)
}

// Draw game elements
function draw() {
    // Draw player ship
    ctx.fillStyle = "#fff";
    ctx.beginPath();
    ctx.arc(playerX, playerY, 10, 0, Math.PI * 2);
    ctx.fill();
    ctx.closePath();
    // Draw other game elements
}

// Event listener for keyboard input
document.addEventListener("keydown", (event) => {
    // Handle key presses for player movement
});

// Start the game loop
gameLoop();
*/